// Import and built-in functions
import utils from './utils.f.js'
import * as helpers from './helpers.f.js'

const data = [1, 2, 3, 4, 5]
const doubled = Array.from(data)
const keys = Object.keys({ a: 1, b: 2 })

export default doubled